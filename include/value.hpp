#pragma once

namespace BulletSize
{
        constexpr float HART_NORMAL_BULLET = 9;
        constexpr float BIG_CIRCLE_RED = 12;
        constexpr float BULLET1 = 5.8;
        constexpr float JUNKO_CORE_BULLET = 9;
}

namespace TextureSize
{
        constexpr float PLAYER_CHARACTER_SIZE_X = 60;
        constexpr float PLAYER_CHARACTER_SIZE_Y = 60;
}

namespace WindowInformation
{
        constexpr float WIDTH = 1366;
        constexpr float HEIGHT = 768;
	constexpr float HALF_WIDTH = WIDTH / 2;
	constexpr float HALF_HEIGHT = HEIGHT / 2;
}

namespace DrawingDepth {
        
}
